<header class="admin group">
  <h2>Shopkick</h2>
  <ul class="right group">
    <li class="has-dropdown">
      <%= (Profile.first ? Profile.first.full_name : current_admin.name).capitalize %>
      <ul class="nav-dropdown">
        <li><%= link_to "Log out", session_url(current_store, subdomain: false), method: :delete %></li>
      </ul>
    </li>
  </ul>
</header>