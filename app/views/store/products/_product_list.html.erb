<% products.each do |product| %>
  <%= render partial: "store/products/product_list_item", locals: { product: product } %>
<% end %>

<%= "No more products..." if products.empty? %>
<%= paginate @products %>