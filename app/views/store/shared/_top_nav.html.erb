<h1 id="logo"><%= current_store.name %></h1>
<nav class="store-top-nav group">
  <ul class="group">
    <li><%= link_to 'Home', root_url %></li>
    <li class='has-dropdown'><%= link_to 'Categories', "#" %>
      <ul class='is-dropdown'>
        <% Category.all.each do |category| %>
          <%= link_to "<li>#{category.title}</li>".html_safe, category_url(category) %>
        <% end %>
      </ul>
    </li>
    <li><%= link_to 'Products', products_url %></li>
    <li><%= link_to 'Cart', cart_url %></li>
  </ul>
</nav>